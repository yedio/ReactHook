import React from 'react';
import styled from 'styled-components';

export default function Header() {
  return <NavWrapper>div</NavWrapper>;
}

const NavWrapper = styled.section`
  height: 100px;
  background-color: gray;
`;
